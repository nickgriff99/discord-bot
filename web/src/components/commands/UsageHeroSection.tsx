export function UsageHeroSection() {
  return (
    <header className="mb-12 max-w-3xl">
      <p className="text-xs font-semibold uppercase tracking-widest text-zinc-500">Usage</p>
      <h1 className="mt-2 font-display text-4xl font-semibold tracking-tight text-white sm:text-5xl">
        In Discord
      </h1>
      <p className="mt-4 text-lg leading-relaxed text-zinc-400">
        Join a voice channel in your server, then use slash commands. The bot only works in servers (not DMs). Most
        playback commands require you to be in the same voice channel you want the bot to use.
      </p>
    </header>
  );
}
